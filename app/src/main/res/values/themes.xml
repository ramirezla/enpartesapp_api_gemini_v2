<resources>
    <!-- Base application theme. -->
    <style name="Theme.Enpartesapp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/my_high_contrast_text_color</item>
        <item name="colorSurface">@color/my_high_contrast_background_color</item>
        <item name="editTextColor">@color/my_high_contrast_text_color</item>
        <item name="colorControlNormal">@color/my_high_contrast_text_color</item>
        <item name="colorControlActivated">@color/my_high_contrast_text_color</item>
        <item name="colorControlHighlight">@color/my_high_contrast_text_color</item>
        <item name="colorOnPrimary">@color/my_high_contrast_text_color</item>
        <item name="colorOnSecondary">@color/my_high_contrast_text_color</item>
    </style>

    <style name="Theme.Enpartesapp.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Enpartesapp.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Enpartesapp.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!-- Base application theme. -->
    <style name="Base.Theme.Enpartesapp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
</resources>